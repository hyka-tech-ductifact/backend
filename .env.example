# Application settings
APP_HOST=localhost
APP_PORT=8080

# Auto-migrate database tables on startup (only enable in development)
AUTO_MIGRATE=true

# Database Configuration
# Use DB_HOST=localhost when running app locally (make app-run)
# Use DB_HOST=postgres when running everything in Docker (make prod-start)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres123
DB_NAME=microservice_db