name: CI

# ─── When it runs ───────────────────────────────────────────
# - PRs to main: new features and fixes (deploys to staging after merge)
# - PRs to release: hotfixes for production (see CONTRIBUTING.md §7)
# - Push to main/release: final validation after merge
# - workflow_call: allows CD to re-run CI before deploying (see cd.yml)
on:
  push:
    branches: [main, release]
  pull_request:
    branches: [main, release]
  workflow_call:

# ─── Shared environment variables ────────────────────────────
# Postgres runs via docker-compose.dev.yml (single source of truth
# for image version, healthcheck, init.sql, and ports).
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  GO_VERSION: "1.24"
  DB_USER: ci_user
  DB_PASSWORD: ci_password
  DB_NAME: ci_db
  DB_HOST: localhost
  DB_PORT: "5432"
  APP_PORT: "8080"
  JWT_SECRET: ci-test-secret-at-least-32-characters-long
  CORS_ORIGINS: "*"
  CONTRACT_VERSION: "0.2.0"
  LOG_LEVEL: error
  LOG_FORMAT: text

# ─── Jobs ────────────────────────────────────────────────────
jobs:

  # ── 0. Branch name & target validation ─────────────────────
  branch-rules:
    name: Branch Rules
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate branch name and target
        env:
          BRANCH: ${{ github.head_ref }}
          BASE: ${{ github.base_ref }}
        run: make validate-branch

  # ── 1. Lint + static analysis ──────────────────────────────
  lint:
    name: Lint & Vet
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install golangci-lint
        run: go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest

      - name: Lint
        run: make lint

  # ── 2. Unit tests ─────────────────────────────────────────
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install gotestsum
        run: go install gotest.tools/gotestsum@latest

      - name: Run unit tests
        run: make test-unit CI=1 COVERAGE=1

      - name: Generate coverage summary
        if: always()
        run: |
          if [ -f coverage.out ]; then
            total=$(go tool cover -func=coverage.out | grep '^total:' | awk '{print $3}')
            echo "## 🧪 Unit Test Coverage: ${total}" > coverage-summary.md
            echo '```' >> coverage-summary.md
            go tool cover -func=coverage.out | grep -v '100.0%' >> coverage-summary.md
            echo '```' >> coverage-summary.md
          fi

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-results
          path: unit-test-results.xml

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: |
            coverage.html
            coverage.out
            coverage-summary.md

  # ── 3. Integration tests (need Postgres) ──────────────────
  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Start Postgres
        run: make db-start

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install gotestsum
        run: go install gotest.tools/gotestsum@latest

      - name: Run integration tests
        run: make test-integration CI=1

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-test-results
          path: integration-test-results.xml

  # ── 4. Contract tests (need Postgres + running API) ────────
  contract-tests:
    name: Contract Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch contract spec
        run: make fetch-contract

      - name: Start Postgres
        run: make db-start

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install gotestsum
        run: go install gotest.tools/gotestsum@latest

      - name: Build and start API server
        run: make app-start

      - name: Run contract tests
        run: make test-contract CI=1

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: contract-test-results
          path: contract-test-results.xml

  # ── 5. E2E tests (need Postgres + running API) ────────────
  e2e-tests:
    name: E2E Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Start Postgres
        run: make db-start

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install gotestsum
        run: go install gotest.tools/gotestsum@latest

      - name: Build and start API server
        run: make app-start

      - name: Run E2E tests
        run: make test-e2e CI=1

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-results
          path: e2e-test-results.xml

  # ── 6. Docker smoke test ─────────────────────────────────────
  docker-smoke:
    name: Docker Smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: make prod-build

      - name: Start containers
        run: make prod-start

      - name: Health check
        run: curl --fail --retry 3 --retry-delay 2 http://localhost:${{ env.APP_PORT }}/api/v1/health

      - name: Teardown
        if: always()
        run: make prod-stop

  # ── 7. Publish test results ────────────────────────────────
  test-results:
    name: Publish Test Results
    if: always()
    needs: [unit-tests, integration-tests, contract-tests, e2e-tests]
    runs-on: ubuntu-latest
    permissions:
      checks: write
    steps:
      - name: Download all test artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: "*-test-results"
          merge-multiple: false

      - name: Publish test results
        uses: dorny/test-reporter@v1
        with:
          artifact: /(.+-test-results)/
          name: Test Summary
          path: "*-test-results.xml"
          only-summary: true
          reporter: java-junit

      - name: Download coverage report
        if: always()
        uses: actions/download-artifact@v4
        with:
          name: coverage-report
        continue-on-error: true

      - name: Publish coverage summary
        if: always()
        run: |
          if [ -f coverage-summary.md ]; then
            cat coverage-summary.md >> $GITHUB_STEP_SUMMARY
          fi
